version: 0.2

phases:

  install:
    runtime-versions:
      python: 3.11

  build:
    commands:
      - echo "Build started"
      - python --version
      - pip install -r requirements.txt
      - python -m py_compile app.py

artifacts:
  files:
    - '**/*'
